apiVersion: v1
kind: Service
metadata:
  name: nodejs-apm-demo
  labels:
    app: nodejs-apm-demo
spec:
  selector:
    app: nodejs-apm-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP

