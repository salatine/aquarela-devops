apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleet-server-http
  namespace: elastic-agent
  labels:
    app: fleet-server-http
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fleet-server-http
  template:
    metadata:
      labels:
        app: fleet-server-http
    spec:
      containers:
      - name: fleet-server-http
        image: docker.elastic.co/elastic-agent/elastic-agent:9.1.3
        env:
          - name: FLEET_ENROLL
            value: "1"
          - name: FLEET_URL
            value: "https://fleet-server.elastic-agent.svc:8220"
          - name: FLEET_ENROLLMENT_TOKEN
            valueFrom:
              secretKeyRef:
                name: fleet-server-token
                key: ENROLLMENT_TOKEN
          - name: FLEET_SERVER_ENABLE
            value: "true"
          - name: FLEET_SERVER_ELASTICSEARCH_HOST
            value: "http://elasticsearch-es-http.logging.svc:9200"
          - name: FLEET_SERVER_SERVICE_TOKEN
            valueFrom:
              secretKeyRef:
                name: fleet-server-service-token
                key: SERVICE_TOKEN
        ports:
          - containerPort: 8220
            name: http
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: fleet-server-http
  namespace: elastic-agent
spec:
  selector:
    app: fleet-server-http
  ports:
    - protocol: TCP
      port: 8220
      targetPort: 8220
  type: ClusterIP
